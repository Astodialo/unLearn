import { Payload, PrivateKey, SignedMessage, KeyHash } from '@lucid-evolution/core-types';

declare function signData(addressHex: string, payload: Payload, privateKey: PrivateKey): SignedMessage;
declare function verifyData(addressHex: string, keyHash: KeyHash, payload: Payload, signedMessage: SignedMessage): boolean;

export { signData, verifyData };
