export declare class ByteString {
    static isStrictInstance(bs: any): bs is ByteString;
    protected _bytes: Uint8Array;
    constructor(bs: string | Uint8Array | ByteString);
    /**
     * @deprecated use `toString()` instead
     */
    get asString(): string;
    toString(): string;
    /**
     * @deprecated use `toBuffer()` instead
     */
    get asBytes(): Uint8Array;
    toBuffer(): Uint8Array;
    clone(): ByteString;
    static fromAscii(asciiStr: string): ByteString;
    static toAscii(bStr: ByteString): string;
    static isValidHexValue(str: string): boolean;
}
