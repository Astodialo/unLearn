/**
 * we have `dataFromString` from "@harmoniclabs/plutus-data"
 *
 * but it just returns data
 * with no way to know how long the original string was
 *
 * so here we basically copy and paste it but with offset
 */ 
