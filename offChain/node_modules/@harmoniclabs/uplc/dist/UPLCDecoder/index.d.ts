import { UPLCProgram } from "../UPLCProgram/UPLCProgram.js";
export type SerializedScriptFormat = "flat" | "cbor";
export declare class UPLCDecoder {
    private constructor();
    static parse(serializedScript: Uint8Array, format?: SerializedScriptFormat, debugLogs?: boolean): UPLCProgram;
}
export declare function parseUPLC(serializedScript: Uint8Array, format?: SerializedScriptFormat, debugLogs?: boolean): UPLCProgram;
